# Security

Do not commit API keys or tokens.

## Supported Secrets
- `MIMO_API_KEY` via `.env` only
- GitHub tokens via local environment only

## Report
Open a private security report or contact maintainer.
